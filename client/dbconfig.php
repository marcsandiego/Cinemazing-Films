<?php
	$hostname = "localhost";
    $server_username = "root";
    $server_password= "";
    $dbName = "cinemazing";

    $connect = mysqli_connect($hostname, $server_username, $server_password, $dbName);

  ?>